org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true

# Gradle 9.1+ is required to run the Gradle daemon on Java 25.
# Application code still compiles with Java 21 via the toolchain in build.gradle.
